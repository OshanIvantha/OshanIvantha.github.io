import React from "react"

import Layout from "../components/layout"

const AboutPage = () => (
    <Layout>
        <h1>About</h1>
        <p>This sections has contact information, education (degrees), about me paragrph etc.</p>
    </Layout>
)

export default AboutPage
